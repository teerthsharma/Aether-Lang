# Electromagnetic Field Data Simulator Web App

React/Vite visualization surface for Aether-Lang's Faraday tensor electromagnetic
coupling simulator.

```bash
npm ci
npm test
npm run build
npm run dev
```

The GitHub Pages workflow builds this directory and deploys `dist` from the
repository `main` branch.
