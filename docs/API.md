# API Reference

The current API map is maintained at [reference/api.md](reference/api.md).

Use that page as the public contract. It separates crate APIs, CLI commands, and
kernel-facing functions without implying that every internal type is stable.
