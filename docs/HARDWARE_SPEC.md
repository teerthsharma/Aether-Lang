# Hardware Specification Status

This compatibility page points to [Hardware Boundary](kernel/hardware-boundary.md).

Any hardware target must document:

- CPU architecture;
- boot path;
- required toolchain;
- run command;
- expected output;
- failure modes;
- captured artifact.

Until those artifacts exist, hardware support should be treated as roadmap.
