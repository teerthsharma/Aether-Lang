[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "aether-lang"
version = "0.1.0"
description = "A topological DSL for machine learning: bounded persistent homology, manifold embeddings, and a sparse-event runtime"
authors = [
    { name = "Teerth Sharma", email = "teerths57@gmail.com" },
]
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Rust",
    "Programming Language :: Python :: Implementation :: CPython",
    "Programming Language :: Python :: Implementation :: PyPy",
    "Topic :: Software Development :: Compilers",
    "Topic :: Software Development :: Interpreters",
    "Intended Audience :: Developers",
    "License :: Other/Proprietary License",
]

[tool.maturin]
# Use the executable name 'aether'
python-source = "bindings/python"
module-name = "aether_lang"


manifest-path = "crates/aether-lang/Cargo.toml"
package = "aether-lang"
features = ["python"]
